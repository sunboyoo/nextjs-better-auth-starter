/**
 * Pagination constants
 */

// Default page size for paginated tables
export const DEFAULT_PAGE_LIMIT = 10;

// Maximum page size allowed for paginated queries
export const MAX_PAGE_LIMIT = 100;

// Page size for dropdown selectors (load more items)
export const SELECTOR_PAGE_LIMIT = 100;

/**
 * Invitation constants
 */

// Number of days before an invitation expires
export const ORGANIZATION_INVITATION_EXPIRES_IN_DAYS = 7;
